   <inbound>
        <!-- Extract Token from Authorization header parameter -->
        <set-variable name="token" value="@(context.Request.Headers.GetValueOrDefault("Authorization","scheme param").Split(' ').Last())" />
        <send-request mode="new" response-variable-name="opaauth" timeout="20" ignore-error="true">
            <set-url>https://opa.icyriver-e6021c38.eastus.azurecontainerapps.io/v1/data/rbac</set-url>
            <set-method>POST</set-method>
            <set-header name="Content-Type" exists-action="override">
                <value>application/json</value>
            </set-header>
            <set-body>@{ 
                    JObject input = new JObject(); 
                    input.Add(new JProperty("token", ((string)context.Variables["token"])));
                    input.Add(new JProperty("operation", ((string)context.Request.Method)));
                    input.Add(new JProperty("route", ((string)context.Request.Url.Path)));

                    JObject body = new JObject{{"input", input}};
                    return body.ToString(); 
                }</set-body>
        </send-request>
        <choose>
            <!-- Check active property in response -->
            <when condition="@((bool)((IResponse)context.Variables["opaauth"]).Body.As<JObject>()["result"]["allow"] == false)">
                <!-- Return 401 Unauthorized with http-problem payload -->
                <return-response>
                    <set-status code="403" reason="Unauthorized" />
                    <set-header name="WWW-Authenticate" exists-action="override">
                        <value>Bearer error="invalid_token"</value>
                    </set-header>
                </return-response>
            </when>
        </choose>
        <base />
    </inbound>